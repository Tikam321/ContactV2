spring:
  application:
    name: contact

  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/contact_db
    username: postgres
    password: postgres

  redis:
    host: localhost
    port: 6379

contact-group:
  max-count: 100
  name-length: 100










